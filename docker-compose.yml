version: '3.9'

services:
  web:
    image: ${DOCKER_IMAGE:-simple-py-webapp}
    build: .
    pull_policy: always
    ports:
      - "5000:5000"
    environment:
      - APP_SECRET_KEY=${APP_SECRET_KEY}
      - FLASK_ENV=production
    restart: unless-stopped